---
import styles from "./Heading.module.scss";

export interface Props {
	text: string;
	level: "h1" | "h2" | "h3" | "h4" | "h5" | "h6";
}
const { text, level } = Astro.props;
const HeadingTag = level;
---

<HeadingTag class={styles.heading}>{text}</HeadingTag>